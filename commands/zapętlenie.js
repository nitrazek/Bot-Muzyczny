const { Client, CommandInteraction } = require('discord.js');

module.exports = {
    name: 'zapętlenie',
    description: 'Zapętla obecną kolejkę',
    /**
     * @param {Client} client 
     * @param {CommandInteraction} interaction 
     */
    async run(client, interaction) {

    }
}